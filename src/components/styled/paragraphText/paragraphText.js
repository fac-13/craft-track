import styled from "styled-components";

const ParagraphText = styled.p.attrs({

}) `
  font-family: 'Roboto Mono', monospace;
  line-height: 1.3;
  font-size: .9rem;
  width: 100%;
  `;

export default ParagraphText;